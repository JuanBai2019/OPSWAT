running in python3

Windows:
In command prompt: 
download pip using one of the following command depends on your python setting
	download pip: python -m pip install --upgrade pip
	download pip: py -m pip install --upgrade pip
download requests package using one of the following command depends on your python setting
	download requests package: python -m pip install requests
	download requests package: py -m pip install requests  

change the API key in line 13
then run the program by one of the following command:
	python ./main.py [testfile_path]		ex: python ./main.py samplefile.txt
	py ./main.py [testfile_path]			ex: py ./main.py samplefile.txt
